Sujay V. Busam
Lab 2

-

Q1: Run the script and pass in two valid date arguments. Program will exit if dates are not valid or not right number of arguments.

-

Q2: Run the script with one valid argument - a year. Exits if invalid argument is entered.

-

Q3: No arguments requried

-

Q4: The program will stop running if there is no url.txt file
(the first argument). The program will complain but continue
onto next URL if the current URL in the file is invalid (will notify user)

The .html files will be overwritten each run.


- 

Q4: Script requires user names as arguments. Will skip invalid names. Exits if invalid arguments. Time periods are calculated in minutes
